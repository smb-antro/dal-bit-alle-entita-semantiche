# Snapshot — cs-1-cartografia-semantica

**4 settembre 2026** — repository appena creato.

- `dominio/` e `ontologia/` copiati integralmente (`git archive`) dai rispettivi
  repository sorgente, al loro stato del 4 settembre 2026 (HEAD `bea0aaa` e `1765f29`
  rispettivamente). Nessuna storia git preservata, nessun file `.git`/`.claude`
  residuo (verificato e corretto un caso: `.gitkeep` di scaffold rimosso da `dominio/`).
- `design-system/` vuota di proposito — vedi `design-system/README.md`.
- `docs/` con decision-log, next-steps, questo snapshot.
- `LICENSE` placeholder con quattro alternative, nessuna scelta ancora fatta.
- Nessun commit ancora fatto in questo repository. Non pubblicato su GitHub. Card del
  portfolio riservata in `portfolio/site/lab.html` ma non collegata.

Prossimo passo reale: linee guida di design (vedi `docs/next-steps.md`, punto 1) — non
ancora iniziato.
